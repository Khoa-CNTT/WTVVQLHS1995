# Cấu hình kết nối trong Rasa

# Cấu hình cho REST channel
rest:
  # Kênh REST sẽ gửi tin nhắn đến URL sau khi nhận được tin nhắn từ người dùng
  # url: "http://localhost:5002/api"

# Cấu hình cho webhook do LegAI cung cấp
webhook:
  url: "http://localhost:8000/api/chatbot/webhook"
  token: ${RASA_WEBHOOK_TOKEN}  # Token xác thực cho webhook

# Cấu hình cho socket.io
socketio:
  user_message_evt: user_uttered
  bot_message_evt: bot_uttered
  session_persistence: true

# Cấu hình cho kênh web chat
rasa:
  url: "http://localhost:5002/api" 